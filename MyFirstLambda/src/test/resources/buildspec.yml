version: 0.2
phases:
  install:
    runtime-versions:
        java: 8
  build:
    commands:
          - aws cloudformation package --template-file template.yml --s3-bucket lamnewbucket --output-template-file outputtemplate.yml
artifacts:
  type: zip
  files:
    - template.yml
    - outputtemplate.yml